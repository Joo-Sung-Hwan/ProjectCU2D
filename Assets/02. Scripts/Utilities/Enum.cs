
// Only Enum

public enum EDetectorType
{
    Nearest,
    Farest,
}

public enum EGrade
{
    Normal = 4,
    Rare = 8,
    Unique = 15,
    Legend = 30
}

public enum EItemType
{
    Exp,
    Magnet,
    Bomb,
}

public enum ESceneType
{
    MainMenu,
    MainGame,
}

public enum EParticleType
{
    BasicHit,
    LightningHit,
    IceHit,
}